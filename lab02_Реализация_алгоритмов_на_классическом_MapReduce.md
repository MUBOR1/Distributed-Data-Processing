# Лабораторная работа №2: Реализация алгоритмов на классическом MapReduce

## Цель работы
Освоить модель программирования **MapReduce** на практике. Получить навыки реализации алгоритмов обработки данных с использованием парадигмы MapReduce на языке **Python** для запуска в **Hadoop Streaming**.

---

## Стек технологий

- **Язык программирования:** Python 3  
- **Фреймворк:** Hadoop Streaming (JAR-утилита)  
- **Платформа / ОС:** Linux (Ubuntu), предустановленные HDFS и Hadoop  
- **Библиотеки (опционально, для локальной отладки):** mrjob  

---

## Теоретическая часть

**MapReduce** — это модель и фреймворк для параллельной обработки больших наборов данных на кластере.  
Вычисления разделяются на две основные фазы:

### 1. Map (Отображение)
- **Вход:** пары ключ–значение (например, *(offset, строка_из_файла)*).  
- **Функция:** пользовательская функция `map()` обрабатывает каждую входную пару и генерирует набор промежуточных пар ключ–значение.  
- **Выход:** список промежуточных пар вида *(промежуточный_ключ, промежуточное_значение)*.

### 2. Reduce (Свёртка)
- **Вход:** пары *(промежуточный_ключ, список_всех_промежуточных_значений)*.  
- **Функция:** пользовательская функция `reduce()` принимает ключ и все связанные с ним значения. Она агрегирует, фильтрует или суммирует эти значения.  
- **Выход:** конечные пары ключ–значение, записываемые в результат.

Между этими фазами framework неявно выполняет этап **Shuffle & Sort**, который группирует все промежуточные значения по ключам.

---

## Практическая реализация

### Задача 1: Классический WordCount

#### Алгоритм
- **Mapper (`mapper_wc.py`)** — читает текст из стандартного ввода, разбивает каждую строку на слова и для каждого слова выдаёт пару *(word, 1)*.  
- **Reducer (`reducer_wc.py`)** — получает на вход пары *(word, [1, 1, 1, ...])*, суммирует единицы и выдаёт итоговую пару *(word, sum)*.

---

### Код реализации

#### `mapper_wc.py`
```python
#!/usr/bin/env python3
import sys

def read_input(file):
    for line in file:
        yield line.split()

def main(separator='\t'):
    data = read_input(sys.stdin)
    for words in data:
        for word in words:
            print('%s%s%d' % (word, separator, 1))

if __name__ == "__main__":
    main()
```

#### `reducer_wc.py`
```python
#!/usr/bin/env python3
import sys

def main(separator='\t'):
    current_word = None
    current_count = 0
    for line in sys.stdin:
        line = line.strip()
        word, count = line.split(separator, 1)
        try:
            count = int(count)
        except ValueError:
            continue

        if current_word == word:
            current_count += count
        else:
            if current_word:
                print('%s%s%d' % (current_word, separator, current_count))
            current_count = count
            current_word = word

    if current_word == word:
        print('%s%s%d' % (current_word, separator, current_count))

if __name__ == "__main__":
    main()
```

---

### Тестовые данные

`input.txt`
```
hello world
hello data
goodbye data
```

---

### Локальная отладка
```bash
cat input.txt | python3 mapper_wc.py | sort | python3 reducer_wc.py
```

**Результат локального тестирования:**
```
data	2
goodbye	1
hello	2
world	1
```

📷 *[СКРИНШОТ 1: Локальный тест WordCount]*
![Альтернативный текст](Локальный тест WordCount.jpg)
![СКРИНШОТ 1: Локальный тест WordCount](./images/Локальный тест WordCount.png)

---

### Запуск в Hadoop Streaming

**Подготовка HDFS:**
```bash
hdfs dfs -mkdir -p /user/student/input_wc
hdfs dfs -put input.txt /user/student/input_wc/
hdfs dfs -rm -r /user/student/output_wc
```

**Команда запуска:**
```bash
hadoop jar $HADOOP_HOME/share/hadoop/tools/lib/hadoop-streaming-*.jar \
-files mapper_wc.py,reducer_wc.py \
-input /user/student/input_wc \
-output /user/student/output_wc \
-mapper "python3 mapper_wc.py" \
-reducer "python3 reducer_wc.py"
```

📷 *[СКРИНШОТ 2: Команда запуска Hadoop Streaming]*  
📷 *[СКРИНШОТ 3: Успешное завершение WordCount в Hadoop]*

**Проверка результатов:**
```bash
hdfs dfs -ls /user/student/output_wc
hdfs dfs -cat /user/student/output_wc/part-00000
```

📷 *[СКРИНШОТ 4: Содержимое выходной директории WordCount в HDFS]*  
📷 *[СКРИНШОТ 5: Результаты WordCount из HDFS]*

---

## Задача 2: Вычисление средней оценки студента по предметам

### Алгоритм
- **Mapper (`mapper_avg.py`)** — для каждой строки генерирует пару *(student_id, grade)*.  
- **Reducer (`reducer_avg.py`)** — для каждого `student_id` вычисляет среднее арифметическое его оценок.

---

### Код реализации

#### `mapper_avg.py`
```python
#!/usr/bin/env python3
import sys

def main(separator='\t'):
    for line in sys.stdin:
        line = line.strip()
        student_id, subject, grade = line.split(',')
        if student_id == 'student_id':
            continue
        try:
            grade = float(grade)
        except ValueError:
            continue
        print('%s%s%f' % (student_id, separator, grade))

if __name__ == "__main__":
    main()
```

#### `reducer_avg.py`
```python
#!/usr/bin/env python3
import sys

def main(separator='\t'):
    current_student = None
    sum_grades = 0
    count_grades = 0

    for line in sys.stdin:
        line = line.strip()
        student_id, grade = line.split(separator, 1)

        try:
            grade = float(grade)
        except ValueError:
            continue

        if current_student == student_id:
            sum_grades += grade
            count_grades += 1
        else:
            if current_student:
                average_grade = sum_grades / count_grades
                print('%s%s%.1f' % (current_student, separator, average_grade))
            current_student = student_id
            sum_grades = grade
            count_grades = 1

    if current_student == student_id:
        average_grade = sum_grades / count_grades
        print('%s%s%.1f' % (current_student, separator, average_grade))

if __name__ == "__main__":
    main()
```

---

### Тестовые данные

`grades.txt`
```
student_id,subject,grade
101,math,92
101,physics,85
102,math,90
102,physics,95
103,math,80
```

---

### Локальная отладка
```bash
cat grades.txt | python3 mapper_avg.py | sort | python3 reducer_avg.py
```

**Результат локального тестирования:**
```
101	88.5
102	92.5
103	80.0
```

📷 *[СКРИНШОТ 6: Локальный тест средней оценки]*

---

### Запуск в Hadoop Streaming

**Подготовка HDFS:**
```bash
hdfs dfs -mkdir -p /user/student/input_avg
hdfs dfs -put grades.txt /user/student/input_avg/
hdfs dfs -rm -r /user/student/output_avg
```

**Команда запуска:**
```bash
hadoop jar $HADOOP_HOME/share/hadoop/tools/lib/hadoop-streaming-*.jar \
-files mapper_avg.py,reducer_avg.py \
-input /user/student/input_avg \
-output /user/student/output_avg \
-mapper "python3 mapper_avg.py" \
-reducer "python3 reducer_avg.py"
```

📷 *[СКРИНШОТ 7: Успешное завершение задачи средней оценки в Hadoop]*

**Проверка результатов:**
```bash
hdfs dfs -cat /user/student/output_avg/part-00000
```

📷 *[СКРИНШОТ 8: Результаты средней оценки из HDFS]*

---

## Ответы на теоретические вопросы

### 1. Почему этап Shuffle & Sort является необходимым и что происходит на этом этапе?

**Необходимость:**
- Гарантирует, что все пары с одинаковым ключом попадут к одному и тому же Reducer’у  
- Обеспечивает корректную агрегацию данных по ключам  
- Позволяет Reducer’ам обрабатывать данные последовательно и эффективно  

**Что происходит:**
- **Shuffle (перемешивание):** промежуточные пары ключ–значение от всех Mapper’ов передаются на узлы с Reducer’ами.  
- **Sort (сортировка):** пары сортируются по ключу, чтобы Reducer мог последовательно обрабатывать все значения для каждого ключа.

Без этого этапа Reducer не смог бы корректно агрегировать данные, так как получал бы разрозненные значения для одного ключа от разных Mapper’ов в произвольном порядке.

---

### 2. В чём заключаются ограничения классической модели MapReduce, которые привели к появлению Spark?

**Основные ограничения:**
- Многократная запись на диск после каждой фазы (Map, Shuffle, Reduce)  
- Неэффективность для итеративных алгоритмов (машинное обучение, графы)  
- Сложность программирования цепочек задач  
- Долгая инициализация задач (неподходит для интерактивных запросов)  
- Жёсткая структура Map–Shuffle–Reduce

**Spark решает эти проблемы за счёт:**
- Использования **RDD (Resilient Distributed Datasets)**  
- Обработки данных в оперативной памяти  
- DAG-оптимизации выполнения  
- Поддержки SQL, ML и стриминга

---

## Заключение

В ходе лабораторной работы были успешно реализованы и протестированы два алгоритма обработки данных на классической модели **MapReduce**.  
Оба алгоритма были запущены как локально, так и в распределённой среде **Hadoop**, что подтверждает корректность реализации и понимание принципов работы MapReduce.
